[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
```

---

### Your Backend Folder Should Now Have These Files
```
backend/
├── app/
│   └── main.py
├── requirements.txt    ✅
├── Procfile            ✅
├── railway.toml        ✅
├── runtime.txt         ✅ add this
├── .python-version     ✅ add this
└── .gitignore